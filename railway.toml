[build]
builder = "nixpacks"

[deploy]
# Runs at both 6:30 and 7:30 UTC — covers 8:30 AM Amsterdam in both CEST and CET
# Python checks the actual Amsterdam time and exits silently if it's the wrong run
cronSchedule = "30 6,7 * * 1-5"
startCommand = "python main.py"
